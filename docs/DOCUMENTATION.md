# Documentation standard

Family contract for the UX stack. Research: Diátaxis, Standard Readme (License last), GitHub community health files, Keep a Changelog.

Canonical pages live under `docs/reference/`, `docs/guides/`, `docs/internals/`, `docs/security/`. Older paths such as `docs/guides/DOCUMENT.md` are **Moved stubs** — do not cite them.

Accuracy: public names from the package surface and CLI `uxdom`. Product `create-app` / `serve` / `deploy` belong in ux-compose.

Out of scope this round: `ux-app`, `ux-motion-lib`.
