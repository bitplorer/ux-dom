"""Removed from product path.

Use ``uxcompose serve`` for product ASGI. This module is intentionally empty.
"""

raise ImportError(
    "ux_dom.cli.serve is not the product path. Use: uxcompose serve app:asgi"
)
